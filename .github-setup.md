# GitHub 上傳指南

## 📋 上傳前檢查清單

在上傳到 GitHub 之前，請確認：

- [x] 已刪除包含真實 API key 的測試文件
- [x] .gitignore 已正確配置
- [x] 所有文件都已建置完成
- [x] README.md 已更新
- [x] INSTALLATION.md 已創建

## 🚀 上傳步驟

### 步驟 1：初始化 Git（如果還沒有）

```bash
# 檢查是否已經是 Git 倉庫
git status

# 如果不是，初始化 Git
git init
```

### 步驟 2：添加所有文件

```bash
# 添加所有文件到 Git
git add .

# 查看將要提交的文件
git status
```

### 步驟 3：提交變更

```bash
# 提交變更
git commit -m "Initial commit: VoiceInput - Voice to Text Assistant"
```

### 步驟 4：在 GitHub 創建新倉庫

1. 前往 [GitHub](https://github.com/)
2. 點擊右上角的 **+** → **New repository**
3. 填寫資訊：
   - **Repository name**: `voice-input-assistant`
   - **Description**: `A macOS floating window app for voice-to-text using OpenAI Whisper API`
   - **Public** 或 **Private**（建議 Public）
   - **不要**勾選 "Initialize this repository with a README"
4. 點擊 **Create repository**

### 步驟 5：連接到 GitHub

```bash
# 添加遠端倉庫（替換 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/voice-input-assistant.git

# 或使用 SSH
git remote add origin git@github.com:YOUR_USERNAME/voice-input-assistant.git

# 驗證遠端倉庫
git remote -v
```

### 步驟 6：推送到 GitHub

```bash
# 推送到 main 分支
git branch -M main
git push -u origin main
```

## 📦 創建 Release（可選）

如果你想要發布預編譯版本：

### 步驟 1：建置應用程式

```bash
# 建置並打包
npm run package:mac
```

### 步驟 2：創建 Release

1. 前往你的 GitHub 倉庫
2. 點擊 **Releases** → **Create a new release**
3. 填寫資訊：
   - **Tag version**: `v1.0.0`
   - **Release title**: `VoiceInput v1.0.0`
   - **Description**: 描述這個版本的功能
4. 上傳檔案：
   - 拖曳 `release/VoiceInput-1.0.0.dmg` 到上傳區域
   - 拖曳 `release/VoiceInput-1.0.0-mac.zip` 到上傳區域
5. 點擊 **Publish release**

## 🔄 後續更新

當你修改代碼後：

```bash
# 查看變更
git status

# 添加變更的文件
git add .

# 提交變更
git commit -m "描述你的變更"

# 推送到 GitHub
git push
```

## ⚠️ 重要提醒

### 不要上傳的內容

以下內容已在 .gitignore 中，不會被上傳：

- `node_modules/` - 依賴套件
- `dist/` - 建置輸出
- `release/` - 打包檔案
- `.env` - 環境變數
- API keys 或敏感資訊

### 檢查敏感資訊

上傳前再次確認：

```bash
# 搜尋可能的 API keys
grep -r "sk-" --exclude-dir=node_modules --exclude-dir=dist .

# 查看將要提交的文件
git diff --cached
```

## 📝 更新 README

記得在 README.md 中更新：

1. 替換所有 `YOUR_USERNAME` 為你的 GitHub 用戶名
2. 更新 GitHub 連結
3. 添加專案截圖（如果有）

## 🎉 完成！

上傳完成後，你的專案將可以在以下位置訪問：

```
https://github.com/YOUR_USERNAME/voice-input-assistant
```

其他人可以：
- 查看源碼
- 下載 Release
- 提出 Issues
- 貢獻代碼（Pull Requests）

---

## 常見問題

**Q: 如何更改倉庫為 Private？**

A: GitHub 倉庫頁面 → Settings → Danger Zone → Change visibility

**Q: 如何刪除已上傳的敏感資訊？**

A: 
```bash
# 從歷史記錄中移除文件
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch PATH_TO_FILE" \
  --prune-empty --tag-name-filter cat -- --all

# 強制推送
git push origin --force --all
```

**Q: 如何添加協作者？**

A: GitHub 倉庫頁面 → Settings → Collaborators → Add people
